<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.praveen.smsapp.MainActivity" >

    <ImageView
        android:id="@+id/main_img_send"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:onClick="sendSms"
        android:scaleType="fitXY"
        android:src="@drawable/send" />

    <ImageView
        android:id="@+id/main_img_receive"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_below="@+id/main_img_send"
        android:onClick="receiveSms"
        android:scaleType="fitXY"
        android:src="@drawable/receive" />

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/main_img_receive"
        android:layout_centerInParent="true"
        android:onClick="notify"
        android:text="Notify" />

</RelativeLayout>